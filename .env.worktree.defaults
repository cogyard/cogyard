# Merge source for per-worktree env (task 042 worktree port management).
# The SessionStart hook copies this file to <worktree>/.env.worktree and
# appends that worktree's allocated PORT and FRONTEND_PORT below.
# scripts/dev.sh sources .env.worktree when present.
